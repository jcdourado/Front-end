/*
	Arrays: var a = [x,x,x];
	
	Da pra acessar por "." e por [], so que por "." ele acessa o valor
	que é referenciado pelo nome

	Da pra acessar parte da string por []

	Metodos strings: 
		length, toUpperCase, toLowerCase, slice(como substring), indexOf
		trim, charAt, 
	
	Metodos array:
		length, push(add no final), pop(remove do fim), join(junta tudo)
		indexOf, unshift(add no inicio), shift(remove no inicio), lastIndexOf
		slice(como o substring), concat, 

	Object: criar como uma var qualquer:
		var d = {  interno : false, right: 2, "name" : nome};
		Metodos: delete propriedade

	For each: for(var v in array){}
	Procurar propriedades no array:
		"valor" in array

	Sempre que se passa um argumento, ele é armazenado em um objeto
	chamado arguments (parecido com Array, mas não é) que corresponde à qtd passada de argumentos

	Math:
		min, sqrt(raiz), max, cos, sin(seno), tan, aos, asin, atan, PI, random
												   Funcoes inversas
												   
		floor(arredonda pra baixo), ceil(cima), round(o mais prox)

*/